//Notación
//; -->Delimita el final de una línea, es una buena práctica
const b = 4;
b + 4;

//. -->Se utiliza en los objetos para acceder a los atributos. Los objetos pueden tener varios atributos
//movil.anchura

//[] --> Listas arreglos o arrays
const ar = [1, 2, 3, 4];
console.log(ar[2]);

//() --> Funciones
function suma(a, b) {
  // Se escibe la función
}

//{} --> Llaves para objetos, funcines y extructuras de control

const movil = {
  anchura: 5,
  altura: 10,
};

if (true) {
  //todo lo que haya entre llaves

  const constante2 = "hola";
}
